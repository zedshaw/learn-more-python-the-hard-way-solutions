#!/usr/bin/env python

from blog.run import main
import sys
import os

_, input_dir, output_dir = sys.argv
assert os.path.exists(input_dir)
assert os.path.exists(output_dir)

main(input_dir, output_dir)

